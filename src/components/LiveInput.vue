<template>
  <!-- <h1 class="hidden">{{ (item = input) }}</h1>
  1233121231231233
  <h1>{{ input }}: {{ getItem }}</h1> -->
  <h1>asd</h1>
</template>

<!-- <script>
import { ref, onMounted } from "vue";

export default {
  props: ["input"],

  setup() {
    const item = ref("");
    const getItem = ref("");

    onMounted(async () => {
      if (localStorage.getItem(item.value)) {
        getItem.value = localStorage.getItem(item.value);
      }
    });

    return {
      item,
      getItem,
    };
  },
};
</script> -->
